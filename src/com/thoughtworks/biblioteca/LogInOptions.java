package com.thoughtworks.biblioteca;

import java.util.Arrays;

public class LogInOptions extends Options {

    public LogInOptions() {
        super(Arrays.asList("Login", "Proceed as guest"));
    }
}
